const crashIconMdStyle = {
   display: { xs: 'none', md: 'flex' },
   mr: 1,
};

const typoGraphyMdStyle = {
   mr: 2,
   display: { xs: 'none', md: 'flex' },
   fontFamily: 'monospace',
   fontWeight: 700,
   letterSpacing: '.3rem',
   color: 'inherit',
   textDecoration: 'none',
};

const iconButtonWrapperStyle = {
   flexGrow: 1,
   display: { xs: 'flex', md: 'none' },
};

const menuStyle = {
   display: { xs: 'block', md: 'none' },
};

const crachIconXsStyle = { display: { xs: 'flex', md: 'none' }, mr: 1 };

const typoGraphyXsStyle = {
   mr: 2,
   display: { xs: 'flex', md: 'none' },
   flexGrow: 1,
   fontFamily: 'monospace',
   fontWeight: 700,
   letterSpacing: '.3rem',
   color: 'inherit',
   textDecoration: 'none',
};

const pagesWrapperStyle = { flexGrow: 1, display: { xs: 'none', md: 'flex' } };
const pageButtonsStyle = { my: 2, color: 'white', display: 'block' };

export {
   crashIconMdStyle,
   typoGraphyMdStyle,
   iconButtonWrapperStyle,
   menuStyle,
   crachIconXsStyle,
   typoGraphyXsStyle,
   pagesWrapperStyle,
   pageButtonsStyle,
};
